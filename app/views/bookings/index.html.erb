  <div class="container-bookings">

    <div class="card-white" id="introduction">
      <% @bookings.each do |booking| %>
      <h2><%= booking.user.name %>'s Bookings</h2>
      <h4>
        <ul>
          <li><%= booking.truck.name %> <%= booking.truck.category %></li>
          <br>
          <li><%= booking.truck.price %></li>
          <% end %>
        </ul>
      </h4>
      <%= link_to "Back to Profile", profile_path %>
    </div>
  </div>
